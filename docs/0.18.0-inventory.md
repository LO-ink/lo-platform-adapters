# SDK 0.18.0 public API inventory

Inventory source: `zaytcevcom/lo-miniapp-sdk` `origin/main` at `15b832edfd03559b12cbb2e843141f10532f5c96`. Native behavior source: LO `miniapp-runtime` at `64c63e9e350ba35c6b96f037b6fab0b21f9047b2`.

The neutral contract targets the breaking `0.19.0` line. It does not reuse the `0.18.0` version for a different API. The referenced `@lo-ink` package releases are published to npm; released-client acceptance remains a separate pending gate.

## Root entrypoint

| 0.18.0 export                                                                    | Migration                                        | Current status                                                                                                                                                                         |
| -------------------------------------------------------------------------------- | ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `detectHost`, `loadHost`                                                         | `createAdapter` / `loadAdapter` in host packages | Implemented. LO never downloads a script; Telegram loader is bounded, coalesced, SSR-safe, and removes its script.                                                                     |
| `Host`, `HostSDK`, `HostButton`, storage/manager types, `Provider`, `HostWindow` | `MiniAppAdapter`, normalized protocol types      | Replaced. Foreign globals and callback shapes remain in adapters.                                                                                                                      |
| `nativeVersion`                                                                  | `isVersionAtLeast` in `@lo-ink/adapter-telegram` | Implemented outside core.                                                                                                                                                              |
| `supports`                                                                       | `client.supports` and `supports`                 | Implemented from the adapter's explicit normalized capability set.                                                                                                                     |
| `subscribe`                                                                      | `client.on` and `subscribe`                      | Implemented with idempotent teardown and client disposal.                                                                                                                              |
| `bindAppearance`                                                                 | `bindAppearance(client, environment)`            | Implemented without discovering globals. Caller supplies its DOM environment.                                                                                                          |
| `withHostCallback`, `MiniAppError`                                               | Same helper plus canonical error codes           | Implemented. Abort code changes from `cancelled` to `aborted`; timeout, late callback, cleanup, and rejection behavior are tested.                                                     |
| `authenticate`, `AppSession`                                                     | Same names, adapter-neutral launch input         | Implemented. Backend callback receives `adapterId` rather than the former provider union. Cache remains launch-bound and revalidated.                                                  |
| `requestWriteAccess`, `requestContact`                                           | Same helpers over `MiniAppClient`                | Implemented with 60 second defaults and Boolean denial semantics.                                                                                                                      |
| `shareMessage`, `shareToStory`, `StoryShareParams`                               | Same helpers with normalized story `link`        | Implemented. Existing length and URL validation preserved; foreign `widget_link` translation is adapter-only.                                                                          |
| `HostSDK.openInvoice`                                                            | `invoice` capability and `openInvoice` operation | Implemented with normalized `paid`, `cancelled`, `failed`, and `pending` results. Current LO adapter omits support; Telegram advertises it only when version and method both allow it. |
| `cloudStorage`, `deviceStorage`, `secureStorage`, `StorageOptions`               | Same facades over typed operations               | Implemented. Defaults remain 30 seconds, except secure storage at 60 seconds. Missing device/secure values remain `null`; cloud storage rejects an invalid missing-value response.     |

## Raw host surface

The former `HostSDK` also exposed APIs without top-level helpers. Every surface has a typed normalized operation in `MiniAppOperationMap` and a compatibility translation:

- lifecycle and chrome: ready, expand, fullscreen enter/exit, keyboard dismissal, orientation lock, closing confirmation, header/background/bottom colors;
- controls: back, main, secondary and settings buttons;
- interaction: haptics, popup, external link, data send, inline query, QR scanner;
- device: clipboard, location, biometry, accelerometer, gyroscope, device orientation, file download;
- invoice, storage, permissions and sharing listed above.

Normalized event coverage includes activation, theme, viewport, both safe areas, fullscreen, four buttons, QR, motion sensors, and device orientation. Subscription cleanup is deterministic.

## Protocol subpath

The old `protocol` entrypoint exported foreign outgoing/incoming wire enums and payloads (`MiniAppOutgoingEvent`, `MiniAppIncomingEvent`, bridge envelope, snake-case payloads, custom storage RPC payloads). Those exports are intentionally absent from the canonical SDK. The replacement exports normalized `Capability`, `MiniAppEventMap`, `MiniAppOperationMap`, snapshots, values, limits, and protocol version. Existing wire schemas remain at the app/adapter edge and are not claimed as LO's canonical public protocol.

## Known gaps in this slice

- Phase 2 extends the current canonical LO transport to 52 operations, 31 capabilities and all 20 normalized events; these counts describe the post-0.19 migration state, not the historical 0.18.0 release. Runtime discovery still requires each operation and its capability to be jointly advertised. Matching-session `LO.WebApp` composition remains only for older ports that omit an operation; a selected native operation never retries through legacy after failure. Older six-operation ports also retain legacy snapshot and appearance-event authority unless they explicitly advertise the modern canonical authority marker.
- Capability groups map as follows: base lifecycle (`ready`, `expand`); chrome and appearance (`fullscreen`, `hideKeyboard`, `orientation`, four buttons, closing confirmation, three colors); interaction (`haptics`, `popup`, `openLink`, `sendData`, `switchInlineQuery`); device (`clipboard`, `location`, `biometry`, `sensors`, `downloadFile`, `qrScanner`); permissions and sharing (`requestWriteAccess`, `requestContact`, `shareMessage`); storage (`cloudStorage`, `deviceStorage`, `secureStorage`).
- `shareToStory` and `invoice` remain absent because the shipped LO host does not implement them. A separate, unmerged story-sharing change is not treated as current capability.
- Current LO runtime advertises only callbacks installed for the active client/runtime/device. Orientation, biometry and sensors retain their existing App runtime gates; protocol version 1 does not imply those device features exist.
- Released-client acceptance remains pending until an App build installs the complete callback set. Transport, adapter, SDK decoding and generated-port integration are covered locally.
- Continuous QR callbacks are exposed as normalized events; no higher-level scanner session object exists yet.
- Existing direct consumers of `host.sdk` require migration to `client.call` or a facade. No foreign-shaped compatibility API is re-exported from core.
- Only ESM adapters are built in this slice. The SDK itself ships ESM, CommonJS, and declarations.
- The VK adapter exercises the same port with the official VK Bridge package, bounded initialization and normalized host events. Local fixtures do not establish live hosted VK authentication or full feature parity.
- Native authentication, permission enforcement, and server protocol remain app-owned and unchanged.

## Exhaustive 0.18.0 symbol list

The root entrypoint exported these types and values: `StoryShareParams`, `Provider`, `HostButton`, `StorageCallback`, `HostCloudStorage`, `HostDeviceStorage`, `HostSecureStorage`, `HostLocationData`, `HostLocationManager`, `HostMotionSensor`, `HostDeviceOrientation`, `HostBiometricManager`, `HostSDK`, `Host`, `HostWindow`, `detectHost`, `loadHost`, `nativeVersion`, `supports`, `subscribe`, `bindAppearance`, `AppSession`, `authenticate`, `MiniAppError`, `withHostCallback`, `requestContact`, `requestWriteAccess`, `StorageOptions`, `cloudStorage`, `deviceStorage`, `secureStorage`, `shareMessage`, and `shareToStory`. `HostSDK.openInvoice` was a public member even though it had no top-level helper.

The `protocol` entrypoint exported: `MiniAppOutgoingEvent`, `MiniAppOutgoingEventName`, `MiniAppIncomingEvent`, `MiniAppIncomingEventName`, `MiniAppBridgeMessage`, `DataSendPayload`, `ClosePayload`, `BottomButtonPayload`, `VisibilityPayload`, `ClosingBehaviorPayload`, `SwipeBehaviorPayload`, `HeaderColorPayload`, `ColorPayload`, `HapticPayload`, `OpenLinkPayload`, `OpenPlatformLinkPayload`, `PopupButton`, `OpenPopupPayload`, `ViewportChangedPayload`, `ThemeChangedPayload`, `InsetPayload`, `PopupClosedPayload`, `VisibilityChangedPayload`, `MiniAppLimits`, `LO_SDK_PROTOCOL_VERSION`, `LO_HOST_CAPABILITIES`, `Capability`, `CloudStorageRequest`, `CloudStorageResult`, `ScanQrPopupPayload`, `QrTextReceivedPayload`, `SensorStartPayload`, `DeviceOrientationStartPayload`, `SensorChangedPayload`, `DeviceOrientationChangedPayload`, `DownloadFileParams`, and `FileDownloadRequestedPayload`. These foreign wire declarations were inventoried rather than copied into the canonical SDK.

Cactus additionally calls `requestChat`, `setEmojiStatus`, and `openTelegramLink`, which were never declared by the 0.18.0 package. They are exposed through `TelegramMiniAppAdapter.telegram` with method/version checks and bounded callbacks. They remain absent from `MiniAppOperationMap`.
